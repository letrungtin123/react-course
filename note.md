npm run: -> chạy 1 câu lệnh trong dự án
npm i (npm install) -> cài đặt thư viện

npm i (install) --save -> cài thư viện vào trong dependencies
npm i (install) --save-dev (-D) -> cài thư viện vào trong devDependencies
